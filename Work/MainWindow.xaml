<Window x:Class="Work.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Work"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="1000"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    <Grid Loaded="Grid_Loaded">
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="登录" Click="btnLogin_Click">

                </MenuItem>
            </Menu>
                <ToolBar DockPanel.Dock="Top" Height="30">
                <Button Content="k" Margin="0 0 0 0"></Button>
                <Button Content="laalla" Margin="90 0 0 0"></Button>
            </ToolBar>
            <Grid>
            <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DataGrid Name="dgTable" Grid.Column="1" Grid.ColumnSpan="5" Grid.RowSpan="4" CellEditEnding="mycehis" BeginningEdit="dgTable_BeginningEdit">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Width="30"/>
                <DataGridTextColumn Width="80" Binding="{Binding rwmc}" Header="rwmc" IsReadOnly="False"></DataGridTextColumn>
                <DataGridTextColumn Width="80" Binding="{Binding tpdj}" Header="tpdj" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Width="90" Binding="{Binding scmc}" Header="scmc" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Width="40" Binding="{Binding xysl}" Header="xysl" IsReadOnly="True"></DataGridTextColumn>
            </DataGrid.Columns>
            <!--<DataGrid.ItemsSource>
                
            </DataGrid.ItemsSource>-->
        </DataGrid>
        </Grid>
        </DockPanel>
    </Grid>
</Window>
